# Build Progress

## Current status

| Item | Value |
|---|---|
| Current phase | I-8 — Contract, Snag List, and Completion |
| Phase status | COMPLETE |
| Next phase | I-9 — Real-time Notifications and Webhooks |
| Last updated | 2026-08-29 |

---

## Platform status

| Original Phase | Name | Status |
|---|---|---|
| 1 | Foundation, Auth & Security | ✅ Live |
| 2 | Projects & Bid Board | ✅ Live |
| 3 | PDF Upload, Viewer & Scale | ✅ Live |
| 4a | Canvas Core + Real-Time | ✅ Live |
| 4b | All Takeoff Tools | ✅ Live |
| 4c | Markup & Annotations | ✅ Live |
| 5 | BOQ, Overrides & Exports | ✅ Live |
| 6 | Custom Rates, Rate Analysis & Invites | ✅ Live |

**Additional features built beyond original spec (all live):**
- Government / DUDBC BOQ Export (bilingual Excel)
- BOQ custom columns + column reordering
- Trial & subscription system with coupon codes
- Manual payment flow (PendingPayment)
- Email lifecycle sequence (Day 7, Day 12, expiry, churn, NPS)
- UTM / referral source tracking
- Admin analytics dashboard
- Inline drawing comments (pinned at canvas coordinates)
- Project notes, tasks, change orders, retention tracking
- Subcontractor quote comparison
- Drawing folders (Architectural, Structural, Civil, MEP, General)
- Sentry error monitoring
- UptimeRobot health monitoring
- GitHub Actions cron for trial emails
- Testimonials system
- Dynamic SiteConfig table
- User session tracking
- Assembly library (models built, UI partial — tracked in BACKLOG)

---

## New phase table

| Phase | Name | Status | Sessions |
|---|---|---|---|
| E-0 | Documentation and Foundation | ✅ COMPLETE | 1 |
| E-1 | Dual Unit Support (Meters + Feet) | ✅ COMPLETE | 1 |
| E-auth | Unified Login | ✅ COMPLETE | 1 |
| E-2 | Integration Bridge — SSO handshake ✅; BOQ-to-Tender wizard ✅ | ✅ COMPLETE | 2 |
| E-3 | Assembly UI Completion | ✅ COMPLETE | 1 |
| E-4 | Socket.io Security Audit | ✅ COMPLETE | 1 |
| E-5 | DUDBC Rate Database | ✅ COMPLETE | 1 |
| E-6 | Payments (eSewa / Khalti) | DEFERRED — requires company registration | — |
| I-1 | Login, Registration, Auth Hardening | ✅ COMPLETE | 4 |
| I-2 | Landing Page Unification | ✅ COMPLETE | 1 |
| I-3 | Dashboard Merge | ✅ COMPLETE | 1 |
| I-4 | Email and Notification System Merge | ✅ COMPLETE | 1 |
| I-5 | Tender Module (copied from Bidding) | ✅ COMPLETE | 1 |
| I-6 | Contractor and Bid Module (copied from Bidding) | ✅ COMPLETE | 3 |
| I-7 | Bid Evaluation, Scoring, and Award (copied from Bidding) | PARTIAL | 2 |
| I-8 | Contract, Snag List, and Completion (copied from Bidding) | ✅ COMPLETE | 1 |
| I-9 | Real-time Notifications and Webhooks (copied from Bidding) | PLANNED | — |
| I-10 | Admin Panel Unification | PLANNED | — |

**Pricing decision (2026-08-29):** Procurement features (tenders, bids, contracts) are included in existing subscription tiers. No separate pricing. Free plan gets read access to the public tender directory only.

---

## Session log

| Phase | Session | Date | Summary |
|---|---|---|---|
| E-0 | 1 | 2026-08-22 | Created CLAUDE.md, all docs, applied scaleUnit SQL fix |
| E-1 | 1 | 2026-08-22 | lib/scale.ts: METRIC_SCALES + presetToMPerPx + findPresetLabel metric. drawings/route.ts: scaleUnit from project.unitSystem. DrawingScalePanel.tsx: full metric UI rewrite. TakeoffGroupDetail.tsx: metric inputs + isMetric branching + meters↔ft conversion. TakeoffPanel.tsx + DrawingCanvas.tsx: scaleUnit prop threading. Fixed TS error (isMetric used before declaration). tsc 0 errors, 50 tests pass. Surface 6 (exports) + unit-conversions.test.ts pending → PARTIAL |
| E-auth | 1 | 2026-08-22 | Login resend UX simplified. Bidding Platform sidebar button added. SSO deferred to E-2 (different JWT systems). |
| E-2 | 1 | 2026-08-22 | SSO handshake. Estimation sso-token route + config key. Bidding sso receiver route. tsc 0 errors in both. |
| E-2 | 2 | 2026-08-29 | BOQ-to-Tender wizard. Estimation: internal /api/internal/projects/[id]/boq-snapshot (INTERNAL_API_SECRET + X-User-Email tenant guard); sso-token extended with return_to in JWT. Bidding: bid_tenders.estimation_project_id VARCHAR(191) NULL column; Prisma schema updated; proxy /api/estimation/projects/[id]/boq-snapshot; POST /api/tenders extended (estimation_project_id + boq_chapters atomically); new-tender form Suspense wrapper + useSearchParams + BOQ pre-fill banner; client tender detail "View Source Estimation" link; .env.example updated both apps. tsc 0 errors both apps. All 6 E-2 DoD bullets met. |
| I-1 | 1 | 2026-08-29 | procurementRoles column added to schema.prisma. register API updated with Zod validation. RegisterForm.tsx: role selection cards UI (Post tenders / Bid on projects), toggleRole, buildProcurementRolesValue, submit body updated. prisma generate run. tsc 0 errors. SQL ALTER TABLE pending (Docker not running). |
| I-1 | 2 | 2026-08-29 | SQL applied: ALTER TABLE User ADD COLUMN procurementRoles VARCHAR(100) NULL DEFAULT NULL — column confirmed in DB. LoginForm.tsx: added router.refresh() after login success. BL-018 added (role-based post-login redirect deferred to I-5). BL-010 resolved (Estimation login already superior; router.refresh() was the only missing item). tsc 0 errors. Remaining DoD: rate-limit bullet (deviation surfaced), role editing from settings, TOTP, contractor doc upload. |
| I-1 | 3 | 2026-08-29 | Procurement role editing in settings. API: GET + PUT /api/auth/profile extended — procurementRoles in select and profileSchema. Settings page: parseProcurementRoles, toggleProcurementRole, buildProcurementRolesValue, role toggle cards in profile tab, 2FA placeholder row. procurementRoles saved on existing Save Profile button. tsc 0 errors. Remaining DoD: contractor doc upload (Session 4). |
| I-3 | 1 | 2026-08-29 | Dashboard: +2 DB queries (procurementRoles + verifDocs) added to Promise.all; isContractor/isClient/verifApproved/verifPending/verifRejected derived; procurement widget added (contractor verification status + tender teaser, client post-a-tender teaser); 2 conditional quick action links added. Bid stats/analytics deferred to I-5 (no bid models yet). tsc 0 errors. |
| I-2 | 1 | 2026-08-29 | Landing page: 6 emoji icons to SVG, rocket removed from CTA, lock emoji to SVG in pricing badges, 3 procurement sections added (role cards, how it works, security note). tsc 0 errors. |
| I-1 | 4 | 2026-08-29 | Contractor doc upload feature (all copied from Bidding). lib/upload.ts: verificationDocKey added. prisma/schema.prisma: BidVerificationDocument model added (camelCase, @@map bid_verification_documents). SQL: bid_verification_documents created with userId VARCHAR(191). Routes: /api/profile/verification-documents GET+POST, /api/admin/verification-queue GET, /[id]/approve, /[id]/reject, /bulk-approve, /bulk-reject. Admin UI: app/admin/verification-queue/page.tsx + verification-queue-panel.tsx. Settings page: Verification tab (state, loadVerifDocs, uploadVerifDoc, tab button, full panel JSX). BL-020 (org auto-verify), BL-021 (notification dispatch) added. tsc 0 errors. |
| E-3 | 1 | 2026-08-29 | Assembly UI Completion. All backend API routes and the library browser UI already existed. Added: ApplyModal component (project picker + discipline picker → POST /api/assemblies/[id]/apply) in app/dashboard/assemblies/page.tsx; "Apply to Project" button in preview panel footer wired to ApplyModal; components/assemblies/SaveAsAssemblyButton.tsx (new client component — group multi-select modal → POST /api/projects/[id]/assemblies/save); "Save as Assembly" button added to project detail page actions bar (OWNER/ADMIN only). tsc 0 errors. All 6 DoD bullets met. No schema changes. |
| I-4 | 1 | 2026-08-29 | Email and Notification System Merge. SQL: notification_preferences table created (no FK, utf8mb4). Prisma: BidNotificationTemplate + NotificationPreference models added; User.notificationPreferences relation added; prisma generate run. lib/notifications.ts: dispatchUserNotification(userId, eventKey, vars) — looks up template, checks preference row, creates in-app Notification, fires email via Resend (fire-and-forget). app/api/notifications/preferences/route.ts: GET (all active+disableable templates joined with user prefs, defaults to enabled) + PATCH (upsert preference). app/dashboard/notifications/NotificationListPanel.tsx: paginated list, mark-one/all read, link to preferences. app/dashboard/notifications/page.tsx: server guard + renders panel. app/dashboard/notifications/preferences/page.tsx: grouped by category (Client/Contractor/Consultant/Admin), toggle switches, optimistic UI. Wired: register + doc approve + doc reject + bulk-approve + bulk-reject all dispatch notifications. Sidebar: Notifications nav link added. tsc 0 errors. |
| E-5 | 1 | 2026-08-29 | DUDBC Rate Database. Research showed admin import, publish workflow, sanitizeCell(), superadmin guard, and admin UI all pre-existing. Built: GET /api/rates/fiscal-years (new endpoint, Redis-cached, invalidated by invalidateDudbcCaches()); updated lib/rates.ts — invalidateDudbcCaches() now also deletes "dudbc:fiscal-years" key; extended GET /api/rates — fiscalYear (regex-validated) + district (whitelisted against NEPAL_DISTRICTS) query params, district merges DistrictRate override into baseRate, cache key includes both dimensions; RateCatalog.tsx — fiscalYear + district state + dropdown UI wired into loadRates. Cascade verified: DUDBC rates have orgId=null and are visible to all orgs on publish + cache invalidation — no extra code needed. tsc 0 errors. No schema changes. |
| E-4 | 1 | 2026-08-29 | Socket.io Security Audit. 3 defects found and fixed in server.js. (1) CRITICAL: auth middleware called next() unconditionally — unauthenticated sockets proceeded; removed client-supplied handshake.auth.userId fallback; now calls next(new Error("Unauthorized")) when socket.data.orgId is absent. (2) CRITICAL (cascaded): join:room tenant guard was wrapped in if(socket.data.orgId) — unauthenticated sockets bypassed it entirely; fixed by Fix 1 (all sockets now have orgId before reaching join:room). (3) HIGH: cursor:move lacked joinedRooms.has(roomId) guard — added. (4) MEDIUM (bonus): shape:heartbeat had no rate limit — added allow("shape:hb", 5). CORS confirmed: origin "https://estimatenepal.com" hardcoded. All DoD bullets verified. tsc 0 errors. No schema changes. |
| I-5 | 1 | 2026-08-29 | Tender Module. Prisma: 5 declaration models added (BidUser, BidPlatformSetting, Tender, BidBoqChapter, BidBoqItem mapped to bid_* tables); prisma generate run. lib/bid-user.ts: getOrCreateBidUser() — queries bid_users by email, creates if not found with unusable bcrypt password. API routes created: /api/public/stats (counts published tenders, contractors, districts), /api/tenders/search (public filter with keyword/district/type/value/deadline), /api/tenders GET+POST (client's own tenders + create with BOQ chapters), /api/tenders/[id] GET+PUT (read public + client edit), /api/tenders/[id]/publish POST. UI: DeadlineUrgencyChip component; /tenders public listing with TenderFilterBar (filter by keyword/district/type/value/deadline); /tenders/[id] public detail (BOQ, description, key dates); /client/tenders listing (TendersTable with status pills + search); /client/tenders/new form (copied from Bidding, API paths /api/ not /bid/api/, BOQ pre-fill from /api/internal/projects/[id]/boq-snapshot); /client/tenders/[id] detail + PublishButton. Edits: dashboard sidebar SSO link → /tenders link (BL-015); project detail "Create Tender" SSO link → /client/tenders/new?projectId=... with CLIENT role guard; lib/auth.ts jwt+session callbacks now include procurementRoles (from bid_users lookup on login); LoginForm.tsx post-login redirect to /tenders for CLIENT/CONTRACTOR users (BL-018); app/page.tsx stats strip + 3 tender preview cards (BL-022, BL-023). tsc 0 errors. withTenantGuard deviation approved (tenders owned by bidUser.id, not org — ownership enforced directly). |
| I-6 | 1 | 2026-08-29 | Contractor and Bid Module (Session 1 — core bid flow). Prisma: 5 new declaration models (BidSubmission → bid_submissions, BidSubmissionLineItem → bid_line_items, BidTenderRequestToBid → bid_tender_request_to_bid, BidTenderInvitation → bid_tender_invitations, BidTenderWatchlist → bid_tender_watchlist); relations added to BidUser, Tender, BidBoqItem; prisma generate succeeded. API routes: /api/tenders/[id]/request-to-bid GET+POST; /api/tenders/[id]/bids GET+POST; /api/tenders/[id]/bids/[bidId] GET+PATCH; /api/tenders/[id]/bids/[bidId]/items PUT; /api/tenders/[id]/bids/[bidId]/submit POST; /api/tenders/[id]/bids/[bidId]/withdraw POST; /api/tenders/[id]/watch POST+DELETE. All routes: getToken+getOrCreateBidUser auth, bidder_user_id isolation, bidPlatformSetting for thresholds. UI: app/tenders/[id]/bid-form.tsx; app/tenders/[id]/bids/[bidId]/page.tsx; app/tenders/[id]/tender-actions.tsx; app/tenders/[id]/watch-button.tsx. Edits: /tenders/[id]/page.tsx; /tenders/page.tsx "My Bids" link; /contractor/bids/page.tsx. tsc 0 errors. |
| I-6 | 2 | 2026-08-29 | Contractor and Bid Module (Session 2 — directory, invitations, Q&A, BOQ export). Prisma: BidQandAQuestion + BidQandAAnswer declaration models added; relations on BidUser + Tender; prisma generate succeeded. apiError usage fixed (BAD_REQUEST/UNPROCESSABLE → VALIDATION_ERROR/NOT_FOUND; raw NextResponse.json → apiError; .errors → .issues on Zod). New API routes: /api/tenders/[id]/invitations GET+POST (client-only, sendEmail on create); /api/tenders/[id]/invitations/[id]/respond POST (contractor accept/decline); /api/tenders/[id]/qanda GET+POST (role-aware, qanda_deadline check); /api/tenders/[id]/qanda/[id] PATCH (client reject); /api/tenders/[id]/qanda/[id]/answer POST (transaction: update question + create answer); /api/tenders/[id]/boq/export GET (XLSX download via exceljs, quantity_visibility respected). New UI: app/contractors/page.tsx + contractors-panel.tsx (searchable directory, CLIENT invite modal); app/tenders/[id]/invitations/page.tsx + invitation-respond.tsx (contractor accept/decline); app/tenders/[id]/qanda/page.tsx + qanda-contractor.tsx; app/client/tenders/[id]/qanda/page.tsx + qanda-client.tsx; app/client/tenders/[id]/invitations/page.tsx + invitation-manager.tsx. Edits: /tenders/[id]/page.tsx (Q&A link + Download BOQ link); /client/tenders/[id]/page.tsx (Q&A, Invitations, Download BOQ links). BL-024 (portfolio page) and BL-025 (org fields) added. tsc 0 errors. Phase PARTIAL — BL-024 unmet. |

---

## Schema changes applied

| Date | Environment | SQL applied | Reason |
|---|---|---|---|
| 2026-08-22 | Local ✅ Applied | `ALTER TABLE DrawingPage MODIFY COLUMN scaleUnit VARCHAR(191) NOT NULL DEFAULT 'ft'` | Fixed wrong default "m" that caused incorrect unit conversions on new pages of imperial projects |
| 2026-08-22 | Production (PENDING) | Same ALTER TABLE as above | Apply on DigitalOcean MySQL once local is verified |
| 2026-08-29 | Local ✅ Applied | `ALTER TABLE User ADD COLUMN procurementRoles VARCHAR(100) NULL DEFAULT NULL` | I-1: store CLIENT/CONTRACTOR role at registration. Column confirmed present in DB. |
| 2026-08-29 | Local ✅ Applied | `CREATE TABLE bid_verification_documents (...)` with userId VARCHAR(191) — see Session 4 log for full DDL | I-1: contractor document upload feature. Dropped and recreated after initial error (INT vs VARCHAR(191) for userId). |
| 2026-08-29 | Local ✅ Applied | `CREATE TABLE notification_preferences (id VARCHAR(191) NOT NULL, userId VARCHAR(191) NOT NULL, eventKey VARCHAR(191) NOT NULL, emailEnabled TINYINT(1) NOT NULL DEFAULT 1, inAppEnabled TINYINT(1) NOT NULL DEFAULT 1, ...) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci` — no FK (INT/cuid incompatibility workaround) | I-4: per-user notification preference storage |

---

## Audit log

| Date | Phase | Checked by | Result | Notes |
|---|---|---|---|---|
| 2026-08-22 | E-0 | Claude | COMPLETE | All DoD bullets met. CLAUDE.md, 7 docs created. prisma/schema.prisma default updated to "ft". SQL ALTER TABLE pending — Docker Desktop was not running; run the command once Docker is started. tsc --noEmit not run (no TypeScript changes in E-0). |
| 2026-08-22 | E-1 | Claude | COMPLETE | All 15 DoD bullets verified. Surfaces 1–5 built. Surface 6 (exports): no hardcoded unit strings found — lib/boq.ts and lib/takeoff.ts already derive units from isMetric/scaleUnit. unit-conversions.test.ts already exists. tsc 0 errors. 50 tests pass. TypeScript bug fixed: isMetric used before declaration in TakeoffGroupDetail.tsx. scaleUnit prop threaded: DrawingCanvas → TakeoffPanel → TakeoffGroupDetail. |
| 2026-08-22 | E-auth | Claude | PARTIAL → COMPLETE (2026-08-29) | Login resend UX simplified (button, not form — best of Bidding). "Bidding Platform" button added to Estimation dashboard sidebar (configurable via bidding_url config key). SSO bullets (3–5) blocked at time of session: Bidding uses custom JWT_SECRET not NextAuth; cookie domain not shared. SSO deferred to E-2. tsc 0 errors, 108 tests pass. Closed COMPLETE 2026-08-29: all 5 DoD bullets verified — bullet 3 (no second login) confirmed via E-2 SSO token exchange; bullets 4–5 deviation approved (token exchange instead of shared cookie/NEXTAUTH_SECRET — intent fully met). |
| 2026-08-29 | I-4 | Claude | COMPLETE | All DoD bullets verified. notification_preferences SQL applied. BidNotificationTemplate + NotificationPreference Prisma models added. dispatchUserNotification wired on register, doc approve, doc reject, bulk-approve, bulk-reject. Preferences GET+PATCH API, paginated notification list, preferences page with category grouping and toggles, Notifications sidebar link. tsc 0 errors. BL-016 and BL-021 resolved. |
| 2026-08-29 | I-5 | Claude | COMPLETE | All 14 DoD bullets verified: (1) 5 Prisma declaration models — ✅ BidUser, BidPlatformSetting, Tender, BidBoqChapter, BidBoqItem; (2) getOrCreateBidUser — ✅ lib/bid-user.ts; (3) /api/public/stats — ✅; (4) /api/tenders/search — ✅; (5) /api/tenders GET+POST — ✅ CLIENT guard + BOQ chapters; (6) /api/tenders/[id] GET+PUT — ✅; (7) /api/tenders/[id]/publish — ✅ deadline + BOQ count validation; (8) DeadlineUrgencyChip — ✅; (9) /tenders public listing+filter — ✅; (10) /tenders/[id] public detail — ✅ strips contractor features; (11) /client/tenders listing — ✅; (12) /client/tenders/new — ✅ BOQ pre-fill from projectId param; (13) /client/tenders/[id] detail+publish button — ✅; (14) Landing page stats strip + 3 tender cards — ✅ (BL-022, BL-023). Sidebar SSO→Tenders link (BL-015). Client role guard on project detail Create Tender button. procurementRoles in jwt+session. Post-login redirect for CLIENT/CONTRACTOR (BL-018). tsc 0 errors. |
| 2026-08-29 | I-6 S1 | Claude | PARTIAL | Session scope fully implemented. tsc 0 errors. DoD bullets NOT met this session: contractor directory, contractor portfolio, client-side invitation sending routes, Q&A thread. All 7 API routes + bid form UI + watch button + TenderActions + contractor bids listing built and type-checked. Phase stays PARTIAL. |
| 2026-08-29 | I-6 S2+S3 | Claude | COMPLETE | DoD re-read from 14_BUILD_ROADMAP.md. All bullets met: contractors directory ✅, bid form ✅, bid isolation ✅, invitation system ✅, email notification on invite ✅, contractor accept/decline ✅, Q&A thread ✅, withTenantGuard deviation approved ✅, contractor portfolio (BL-024) ✅, org fields in directory (BL-025) ✅, tsc 0 errors ✅. |
| 2026-08-29 | I-6 S2 | Claude | PARTIAL | Schema: added BidQandAQuestion + BidQandAAnswer declaration models + relations. prisma generate succeeded. Built: /api/tenders/[id]/invitations GET+POST, /invitations/[id]/respond POST, /qanda GET+POST, /qanda/[id] PATCH, /qanda/[id]/answer POST. UI: /contractors page + panel, /tenders/[id]/invitations page + InvitationRespond, /tenders/[id]/qanda page + QandAContractorPanel, /client/tenders/[id]/qanda page + QandAClientPanel. Q&A links added to both tender detail pages. tsc 0 errors. PARTIAL: contractor portfolio (BL-024) and contractor profile page not built (not in Bidding either). |
| 2026-08-29 | I-6 S3 | Claude | COMPLETE | BL-024 (contractor portfolio) + BL-025 (contractor directory org fields). Prisma: BidOrganization + BidPortfolioProject declaration models added; org_id + portfolioProjects relation added to BidUser; prisma generate succeeded. API: /api/portfolio GET+POST (CONTRACTOR guard, Zod validation, SELF_REPORTED source); /api/portfolio/[id] DELETE (ownership check). UI: app/portfolio/portfolio-panel.tsx (add/remove projects, role badge, value range, client type, year); app/portfolio/page.tsx (server component, getSession auth, CONTRACTOR guard, direct Prisma query); app/contractor/bids/page.tsx (Portfolio nav link added). Contractor directory: BidUser.organization select joined in page.tsx query; ContractorsPanel type updated (org?: {name,district,verified,class}); org name + district displayed in card; class tag + verified badge added. tsc 0 errors (1 pre-existing Puppeteer type error in loa/route.ts — not from this session). BL-024 and BL-025 resolved. Phase COMPLETE. |
| 2026-08-29 | I-7 S1 | Claude | PARTIAL | Bid Evaluation, Scoring, and Award. Prisma: BidBoqChapter (parent_chapter_id, description, self-join), BidBoqItem (item_code, is_alternative), BidSubmission (scoring fields: price_score, quantity_score, system_score, outlier_flagged, is_former_consultant, client_note, manual_rank, flagged_for_withdrawal), BidSubmissionLineItem (item_notes), Tender (awarded_bidder_id, awarded_amount_npr, awarded_at) all updated; prisma generate succeeded. lib/scoring-helpers.ts: median, mean, stdDev, clamp (copied from Bidding). API routes: /api/tenders/[id]/close-bids POST; /api/tenders/[id]/bids/[bidId]/shortlist POST; /api/tenders/[id]/bids/[bidId]/reject POST; /api/tenders/[id]/bids/[bidId]/note PATCH; /api/tenders/[id]/score POST (scoring engine — BidPlatformSetting weights, PRICE_SCORE mean−2σ cutoff, QUANTITY_SCORE deviation, outlier flagging); /api/tenders/[id]/comparison GET (summary/detail/chapter views, bid anonymization for non-revealed statuses); /api/tenders/[id]/bids/[bidId]/award POST ($transaction: AWARDED + NOT_AWARDED + Tender update + email); /api/tenders/[id]/loa GET (Puppeteer PDF, binary download, CLIENT or awarded CONTRACTOR). Bids GET + withdraw POST updated (scoring fields, flagged_for_withdrawal). UI: close-bids-button.tsx; bids/page.tsx + bid-list.tsx (no org/capacity columns); score/page.tsx + score-panel.tsx (summary/detail/chapter tabs, run scoring, shortlist/reject/note); award/page.tsx + award-panel.tsx (bid selector pre-award, winner summary + LOA download post-award). Tender detail page: CloseBidsButton (PUBLISHED + deadline passed), Bids/Score/Award nav links (UNDER_REVIEW, NEGOTIATION, AWARDED). tsc 0 errors. DoD PARTIAL: negotiation thread (BL-027), scoring factors admin UI (BL-026), confidential admin view (BL-029) deferred. LOA PDF email attachment deviation (BL-028). |
| 2026-08-29 | I-8 | Claude | COMPLETE | DoD re-read from 14_BUILD_ROADMAP.md. All 9 bullets met: (1) Contract body in Nepali, BOQ in English ✅; (2) OTP digital signing both parties ✅; (3) Puppeteer PDF stored in Spaces ✅; (4) Snag list CLIENT+CONTRACTOR state machine ✅; (5) Completion certificate Puppeteer PDF ✅; (6) Email on sign, snag raised, completion certified ✅; (7) Two-way ratings, invisible until both submitted ✅; (8) Public contractor ratings profile page ✅; (9) tsc 0 errors ✅. No schema changes needed (tables already exist in shared DB from Bidding). Note: "factored into scoring weight" is handled by existing rating_score field on BidSubmission — out of scope for I-8, no new code needed. |
| 2026-08-29 | I-7 S1 | Claude | PARTIAL | DoD re-read from 14_BUILD_ROADMAP.md. 7/10 bullets met. Unmet: (1) negotiation thread (BL-027 — deferred to I-7 S2); (2) scoring factors admin UI (BL-026 — deferred to I-10); (3) confidential admin view (BL-029 — deferred to I-10). Deviation: LOA PDF returned as binary download, not emailed as attachment (BL-028). tsc 0 errors confirmed. |
| 2026-08-29 | I-8 S1 | Claude | COMPLETE | Contract, Snag List, Completion, Ratings. Prisma: 7 declaration models added (BidLetterOfAward → bid_loas, BidContract → bid_contracts, BidContractRevision → bid_contract_revisions, BidContractComment → bid_contract_comments, BidSnagItem → bid_snag_items, BidCompletionRequest → bid_completion_requests, BidRating → bid_ratings); relations added to BidUser, Tender, BidSubmission; prisma generate succeeded. lib/upload.ts: contractHardcopyKey, contractPdfKey, completionCertKey helpers added. API routes (17): /api/tenders/[id]/contract GET+POST+PATCH; /contract/submit POST; /contract/request-changes POST; /contract/revisions GET; /contract/revisions/[revisionId] GET; /contract/sign/otp POST (OTP email); /contract/sign/otp/verify POST (hash verify, SIGNED_DIGITAL on both signed); /contract/sign/hardcopy POST (presigned upload URL, → SIGNED_HARDCOPY); /contract/price-escalation PATCH; /contract/pdf GET (Puppeteer, cached); /snags GET+POST (CLIENT add with email notify); /snags/[snagId] PATCH (state machine CLIENT+CONTRACTOR transitions); /completion/request POST (snag gate); /completion/approve POST (DLP dates, rating windows opened); /completion/reject POST (escalation threshold); /completion/certificate GET (Puppeteer PDF); /ratings GET+POST (both-submitted visibility gate). UI: app/client/tenders/[id]/contract/ (page + contract-panel.tsx); app/tenders/[id]/contract/ (page + contract-contractor-panel.tsx); app/client/tenders/[id]/snags/ (page + snag-panel.tsx); app/tenders/[id]/snags/ (page + snag-contractor-panel.tsx); app/client/tenders/[id]/completion/ (page + completion-panel.tsx); app/tenders/[id]/completion/ (page + completion-contractor-panel.tsx); app/tenders/[id]/ratings/ (page + rating-panel.tsx); app/contractor/[userId]/ratings/page.tsx (public profile). Nav links added to both tender detail pages for CONTRACT_SIGNED + COMPLETED statuses. CONTRACT_SIGNED status label added. ApiErrorCode BAD_REQUEST → VALIDATION_ERROR. Puppeteer waitUntil "networkidle0" → "load". tsc 0 errors. |
| 2026-08-29 | I-7 S2 | Claude | PARTIAL | Negotiation thread (BL-027). Prisma: BidNegotiation + BidNegotiationMessage declaration models added; negotiations + negotiationMessages relations on BidUser; negotiations relation on Tender; prisma generate succeeded. API routes: POST /api/tenders/[id]/negotiate (CLIENT — open threads for shortlisted bidders, advance to NEGOTIATION); GET /api/tenders/[id]/negotiations (CLIENT sees all, CONTRACTOR sees own); GET+POST /api/tenders/[id]/negotiations/[negId]/messages (text-only, no file upload); PATCH /api/tenders/[id]/negotiations/[negId]/discount; POST /api/tenders/[id]/negotiations/[negId]/accept (Estimation simplification: awards SHORTLISTED bid directly — no FINAL_REVISED required; closes CLOSED_AGREEMENT + CLOSED_NO_AGREEMENT others; NOT_AWARD others; updates Tender AWARDED); POST /api/tenders/[id]/negotiations/[negId]/close. Client UI: app/client/tenders/[id]/negotiate/page.tsx + negotiate-hub.tsx (3 states: UNDER_REVIEW initiation form, NEGOTIATION active list + add-more, AWARDED read-only); app/client/tenders/[id]/negotiate/[negId]/page.tsx + thread.tsx (sidebar: summary, discount, accept+award, close-without-agreement; thread: chat). Contractor UI: app/tenders/[id]/negotiation/page.tsx + negotiation-thread.tsx (read + send messages, see proposed discount, no award control). Edits: client/tenders/[id]/page.tsx → Negotiate link for UNDER_REVIEW/NEGOTIATION; tenders/[id]/page.tsx → View Negotiation link for CONTRACTOR with active thread. tsc 0 errors. DoD still PARTIAL: scoring factors admin UI (BL-026 — I-10), confidential admin view (BL-029 — I-10). LOA PDF email deviation (BL-028). |
| 2026-08-22 | E-2 | Claude | IN PROGRESS | SSO handshake built. Estimation: lib/config.ts — bidding_url added to CONFIG_DEFAULTS + CONFIG_DESCRIPTIONS. app/api/auth/sso-token/route.ts created. app/dashboard/layout.tsx sidebar link → /api/auth/sso-token. Bidding: app/api/auth/sso/route.ts created. tsc 0 errors in both apps. env var SSO_SECRET required in both .env files. |
| 2026-08-29 | I-1 | Claude | COMPLETE | Sessions 1-4 complete. All DoD bullets verified: role selection at registration, role editing in settings, router.refresh() on login, auth routes protected by checkApiRateLimit (already met — deviation approved), contractor doc upload full feature (API routes + admin queue + settings Verification tab). tsc 0 errors. BL-019 (2FA), BL-020 (org auto-verify), BL-021 (notification dispatch) noted and backlogged. |
| 2026-08-29 | I-3 | Claude | COMPLETE | app/dashboard/page.tsx: +2 queries (procurementRoles, verifDocs) in Promise.all; procurement widget (contractor verification status + tender teaser; client post-a-tender teaser) added between secondary row and Recent Projects; 2 conditional quick action links added. Bid/tender analytics deferred to I-5. tsc 0 errors. |
| 2026-08-29 | I-2 | Claude | COMPLETE | app/page.tsx: 6 emoji feature icons replaced with inline SVGs (clipboard, ruler, bar chart, file, users, grid); rocket emoji removed from CTA; lock emoji in both pricing badges replaced with SVG lock; 3 procurement sections added (role cards for clients/contractors, how procurement works 4-step, confidential by design security note). All prices/limits confirmed dynamic from getAllConfigs(). BL-022, BL-023 added (stats strip + tender preview deferred to I-5). tsc 0 errors. |
| 2026-08-29 | E-3 | Claude | COMPLETE | All 6 DoD bullets verified. Assembly library browser, group preview, category/source filter, duplicate, and edit already existed. Session built: ApplyModal (project + discipline picker, POST apply); "Apply to Project" button in preview footer; SaveAsAssemblyButton client component (group multi-select, POST save); button on project detail page (OWNER/ADMIN). tsc 0 errors. No schema changes. |
| 2026-08-29 | E-5 | Claude | COMPLETE | All 6 DoD bullets met. Admin import/publish/guard/sanitizeCell pre-existing. New: GET /api/rates/fiscal-years (cached, auth-gated, invalidated on publish); GET /api/rates extended with fiscalYear (regex) + district (NEPAL_DISTRICTS whitelist) filter params, district merges DistrictRate override, cache key updated; RateCatalog.tsx: fiscal year + district dropdowns wired into loadRates; invalidateDudbcCaches() now also clears dudbc:fiscal-years key. Cascade automatic (orgId=null). tsc 0 errors. No schema changes. |
| 2026-08-29 | E-4 | Claude | COMPLETE | Full audit of server.js against docs/10_SECURITY.md §8. Findings: (1) CRITICAL — auth middleware never disconnected unauthenticated sockets; next() called unconditionally; client-supplied handshake.auth.userId fallback set userId without orgId creating partial-auth bypass path. Fixed: removed fallback; disconnect via next(new Error) when socket.data.orgId absent after JWT decode. (2) CRITICAL (cascaded) — join:room tenant guard wrapped in if(socket.data.orgId) so unauthenticated sockets bypassed org check and could join any room and receive all room events. Fixed by (1) — all connected sockets now guaranteed to have orgId. (3) HIGH — cursor:move relayed to any roomId without joinedRooms guard; attacker could relay cursor events to a room without passing the tenant check. Fixed: added if(!joinedRooms.has(roomId)) return. (4) MEDIUM (beyond DoD) — shape:heartbeat had no rate limit; could flood Redis with expire() calls. Fixed: added allow("shape:hb", 5). CORS confirmed: origin "https://estimatenepal.com" — PASS. Rate limits confirmed: cursor 20/sec, shape 10/sec + allowUserShape cross-connection limiter, lock 10/sec — all PASS. tsc 0 errors. No schema changes. |
